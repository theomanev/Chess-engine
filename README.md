# Chess engine application

A single-player chess application with a GUI implemented via Java Swing. The application includes an artificially intelligent opponent powered by the minimax algorithm, and alpha-beta pruning is used to reduce the number of candidate moves searched by the engine.
